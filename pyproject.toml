[project]
name = "historical-monetary-data"
version = "0.1.0"
description = "Primary-source monetary data, transcribed from page images"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas",
    "matplotlib",
    "ipykernel",
    "requests>=2.33.1",
    "statsmodels>=0.14.6",
    "jinja2>=3.1.6",
    "xlrd>=2.0.2",
]
